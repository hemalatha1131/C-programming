7. Write a C program to calculate salary after adding bonus.
#include <stdio.h>

int main() {
    float salary, bonus, totalSalary;

    printf("Enter basic salary: ");
    scanf("%f", &salary);

    printf("Enter bonus amount: ");
    scanf("%f", &bonus);

    totalSalary = salary + bonus;

    printf("Total Salary after bonus = %.2f", totalSalary);

    return 0;
}
