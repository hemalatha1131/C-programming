41. Write a C program to check whether a password is valid.
#include <stdio.h>
#include <string.h>

int main() {
    char pwd[50];
    printf("Enter password: ");
    scanf("%s", pwd);

    if (strlen(pwd) >= 6)
        printf("Password is valid");
    else
        printf("Password is invalid");

    return 0;
}
