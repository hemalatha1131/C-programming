33. Write a C program to calculate total weekly sales.
#include <stdio.h>
int main() {
    int sales[7], i, total = 0;

    for(i = 0; i < 7; i++) {
        scanf("%d", &sales[i]);
        total += sales[i];
    }

    printf("Total weekly sales = %d", total);
    return 0;
}
