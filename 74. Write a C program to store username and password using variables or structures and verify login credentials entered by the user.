74. Write a C program to store username and password using variables or structures and verify
login credentials entered by the user.
#include <stdio.h>
#include <string.h>

int main() {
    char username[20] = "admin";
    char password[20] = "1234";
    char u[20], p[20];

    printf("Enter username: ");
    scanf("%s", u);
    printf("Enter password: ");
    scanf("%s", p);

    if(strcmp(u, username) == 0 && strcmp(p, password) == 0)
        printf("Login Successful\n");
    else
        printf("Invalid Login\n");

    return 0;
}
