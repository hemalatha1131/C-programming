42. Write a C program to count characters in a string.
#include <stdio.h>

int main() {
    char str[100];
    int count = 0;

    printf("Enter a string: ");
    gets(str);

    while (str[count] != '\0')
        count++;

    printf("Number of characters: %d", count);
    return 0;
}
