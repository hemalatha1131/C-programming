39. Write a C program to find topper in a class.

#include <stdio.h>
int main() {
    int marks[10], n, i, top;

    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d", &marks[i]);

    top = marks[0];
    for(i = 1; i < n; i++) {
        if(marks[i] > top)
            top = marks[i];
    }

    printf("Topper marks = %d", top);
    return 0;
}
