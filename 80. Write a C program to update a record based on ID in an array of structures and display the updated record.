80. Write a C program to update a record based on ID in an array of structures and display the
updated record.
#include <stdio.h>

struct emp {
    int id;
    char name[20];
};

int main() {
    int i, n, id;
    struct emp e[10];

    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d %s", &e[i].id, e[i].name);

    scanf("%d", &id);

    for(i = 0; i < n; i++) {
        if(e[i].id == id) {
            scanf("%s", e[i].name);
            printf("Updated\n");
            break;
        }
    }

    return 0;
}
