79. Write a C program to search for a record by ID from an array of structures and display the
details if found.
#include <stdio.h>

struct emp {
    int id;
    char name[20];
};

int main() {
    int i, n, id;
    struct emp e[10];

    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d %s", &e[i].id, e[i].name);

    scanf("%d", &id);

    for(i = 0; i < n; i++) {
        if(e[i].id == id) {
            printf("%d %s\n", e[i].id, e[i].name);
            return 0;
        }
    }

    printf("Record not found\n");
    return 0;
}
