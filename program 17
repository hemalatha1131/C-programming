17. Write a C program to check whether a customer gets a discount.
#include <stdio.h>

int main() {
    float amount;
    printf("Enter total bill amount: ");
    scanf("%f", &amount);

    if (amount >= 500)
        printf("Customer gets discount");
    else
        printf("No discount");

    return 0;
}
